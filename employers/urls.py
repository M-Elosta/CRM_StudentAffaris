"""
URL patterns for the employers app.

Sections are added here progressively as each step is built.
All views require login (enforced in views.py).
"""

from django.urls import path

urlpatterns = [
    # Populated in subsequent build steps.
]
